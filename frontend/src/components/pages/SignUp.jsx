import React from 'react';

/**
 * PLACEHOLDER — Member 3 (Bhoomika) builds the real version.
 * Expected structure (from task breakdown):
 *
 * function SignUp() {
 *   const [form, setForm] = useState({
 *     fullName: '', email: '', password: '', confirmPassword: ''
 *   });
 *
 *   const handleSubmit = async (e) => {
 *     e.preventDefault();
 *     // validate fields, call register API, then navigate('/login')
 *   };
 *
 *   return (
 *     <form onSubmit={handleSubmit}>
 *       <InputField ... />
 *       <PasswordStrengthMeter password={form.password} />
 *       <button type="submit">Sign Up</button>
 *     </form>
 *   );
 * }
 */
function SignUp() {
  return (
    <div className="container py-5 text-center text-muted">
      <h3>Sign Up Page — coming soon</h3>
      <p>(Placeholder — Member 3 to implement)</p>
    </div>
  );
}

export default SignUp;
